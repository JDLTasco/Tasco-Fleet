# Postgres connection string — Railway provides this automatically as DATABASE_URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fleet_dev

# Random 32+ char secret for signing session cookies. Generate with: openssl rand -hex 32
AUTH_SECRET=change-this-to-a-long-random-string
